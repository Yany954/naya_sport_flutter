/* import 'package:flutter/material.dart';
import 'package:responsive_grid/responsive_grid.dart';
import 'package:responsive_grid_list/responsive_grid_list.dart';

/* 
class GridViewContainer extends StatelessWidget {
  const GridViewContainer({super.key});
 */
 /*  @override
  Widget build(BuildContext context) {
    return ResponsiveGridListBuilder(
      // Set desired grid pattern (e.g., ResponsiveGridPattern.twoColumns)
      pattern: ResponsiveGridPattern.twoColumns,
      // Customize spacing and padding as needed
      padding: const EdgeInsets.all(8.0),
      // Provide builder function to define grid items
      itemBuilder: (context, index) => Container(
          // ... your item content
          ),
      // Set item count
      itemCount: 10, // Replace with actual number of items
    );
  } 
}*/
 */